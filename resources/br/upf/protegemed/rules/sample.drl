package br.upf.protegemed.rules
 
import br.upf.protegemed.utils.Utils;
import br.upf.protegemed.beans.CapturaAtual;
 
rule "Teste"
    when
        m : CapturaAtual( getCodCaptura() == 2737,myMessage : "Executou regra " )
    then
        Utils.logger(myMessage + drools.getRule().getName());
end